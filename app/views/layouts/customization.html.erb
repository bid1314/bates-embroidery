<!DOCTYPE html>
<html>
  <head>
    <title>Product Customizer - Bates Embroidery</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    
    <!-- TUI Image Editor CSS -->
    <link rel="stylesheet" href="https://uicdn.toast.com/tui-image-editor/latest/tui-image-editor.css">
    <link rel="stylesheet" href="https://uicdn.toast.com/tui-color-picker/latest/tui-color-picker.css">
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    
    <style>
      body {
        margin: 0;
        padding: 0;
        overflow: hidden;
      }
      
      .customizer-container {
        height: 100vh;
        display: flex;
        flex-direction: column;
      }
      
      .customizer-header {
        background: #343a40;
        color: white;
        padding: 10px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 1000;
      }
      
      .customizer-content {
        flex: 1;
        display: flex;
        overflow: hidden;
      }
      
      .product-preview {
        flex: 1;
        background: #f8f9fa;
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      
      .design-tools {
        width: 350px;
        background: white;
        border-left: 1px solid #dee2e6;
        overflow-y: auto;
      }
      
      .tui-image-editor-container {
        position: absolute !important;
        top: 0;
        left: 0;
        right: 350px;
        bottom: 0;
      }
      
      .product-image-container {
        position: relative;
        max-width: 500px;
        max-height: 600px;
      }
      
      .product-image {
        width: 100%;
        height: auto;
        border-radius: 8px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      }
      
      .design-overlay {
        position: absolute;
        top: 20%;
        left: 20%;
        width: 60%;
        height: 60%;
        border: 2px dashed #007bff;
        background: rgba(0, 123, 255, 0.1);
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s ease;
      }
      
      .design-overlay:hover {
        background: rgba(0, 123, 255, 0.2);
        border-color: #0056b3;
      }
      
      .design-overlay.active {
        border-color: #28a745;
        background: rgba(40, 167, 69, 0.1);
      }
      
      .tool-section {
        padding: 20px;
        border-bottom: 1px solid #dee2e6;
      }
      
      .tool-section h6 {
        margin-bottom: 15px;
        color: #495057;
        font-weight: 600;
      }
      
      .btn-tool {
        width: 100%;
        margin-bottom: 10px;
        text-align: left;
      }
      
      .color-picker-container {
        display: flex;
        flex-wrap: wrap;
        gap: 5px;
        margin-top: 10px;
      }
      
      .color-swatch {
        width: 30px;
        height: 30px;
        border-radius: 4px;
        border: 2px solid #dee2e6;
        cursor: pointer;
        transition: transform 0.2s;
      }
      
      .color-swatch:hover {
        transform: scale(1.1);
      }
      
      .color-swatch.active {
        border-color: #007bff;
        transform: scale(1.1);
      }
    </style>
  </head>

  <body>
    <div class="customizer-container">
      <!-- Header -->
      <div class="customizer-header">
        <div class="d-flex align-items-center">
          <%= link_to spree.product_path(@product), class: "btn btn-outline-light btn-sm me-3" do %>
            <i class="fas fa-arrow-left me-1"></i> Back to Product
          <% end %>
          <h5 class="mb-0">Customize: <%= @product.name %></h5>
        </div>
        <div class="d-flex gap-2">
          <button class="btn btn-outline-light btn-sm" id="save-design">
            <i class="fas fa-save me-1"></i> Save Design
          </button>
          <button class="btn btn-primary btn-sm" id="add-to-cart">
            <i class="fas fa-shopping-cart me-1"></i> Add to Cart
          </button>
        </div>
      </div>
      
      <!-- Main Content -->
      <div class="customizer-content">
        <!-- Product Preview Area -->
        <div class="product-preview">
          <div class="product-image-container">
            <% if @product.images.any? %>
              <%= image_tag @product.images.first.url(:large), class: "product-image", id: "product-image" %>
            <% else %>
              <div class="product-image bg-light d-flex align-items-center justify-content-center" style="width: 400px; height: 500px;">
                <i class="fas fa-image fa-5x text-muted"></i>
              </div>
            <% end %>
            
            <!-- Design Overlay Area -->
            <div class="design-overlay" id="design-area">
              <div class="text-center text-muted">
                <i class="fas fa-plus fa-2x mb-2"></i>
                <div>Click to start designing</div>
              </div>
            </div>
          </div>
          
          <!-- Hidden TUI Image Editor Container -->
          <div id="tui-image-editor" style="display: none;"></div>
        </div>
        
        <!-- Design Tools Panel -->
        <div class="design-tools">
          <!-- Text Tools -->
          <div class="tool-section">
            <h6><i class="fas fa-font me-2"></i>Text</h6>
            <button class="btn btn-outline-primary btn-tool" id="add-text">
              <i class="fas fa-plus me-2"></i>Add Text
            </button>
            <div class="mt-3">
              <label class="form-label small">Text Content</label>
              <input type="text" class="form-control form-control-sm" id="text-input" placeholder="Enter text...">
            </div>
            <div class="mt-2">
              <label class="form-label small">Font Size</label>
              <input type="range" class="form-range" id="font-size" min="12" max="72" value="24">
              <small class="text-muted">24px</small>
            </div>
          </div>
          
          <!-- Logo/Image Tools -->
          <div class="tool-section">
            <h6><i class="fas fa-image me-2"></i>Logo & Images</h6>
            <button class="btn btn-outline-primary btn-tool" id="upload-logo">
              <i class="fas fa-upload me-2"></i>Upload Logo
            </button>
            <input type="file" id="logo-upload" accept="image/*" style="display: none;">
            
            <% if user_signed_in? %>
              <button class="btn btn-outline-secondary btn-tool" id="my-logos">
                <i class="fas fa-folder me-2"></i>My Logos
              </button>
            <% end %>
          </div>
          
          <!-- Color Tools -->
          <div class="tool-section">
            <h6><i class="fas fa-palette me-2"></i>Colors</h6>
            <div class="color-picker-container">
              <div class="color-swatch" style="background-color: #000000;" data-color="#000000"></div>
              <div class="color-swatch" style="background-color: #ffffff;" data-color="#ffffff"></div>
              <div class="color-swatch" style="background-color: #ff0000;" data-color="#ff0000"></div>
              <div class="color-swatch" style="background-color: #00ff00;" data-color="#00ff00"></div>
              <div class="color-swatch" style="background-color: #0000ff;" data-color="#0000ff"></div>
              <div class="color-swatch" style="background-color: #ffff00;" data-color="#ffff00"></div>
              <div class="color-swatch" style="background-color: #ff00ff;" data-color="#ff00ff"></div>
              <div class="color-swatch" style="background-color: #00ffff;" data-color="#00ffff"></div>
            </div>
            <div class="mt-3">
              <label class="form-label small">Custom Color</label>
              <input type="color" class="form-control form-control-color" id="custom-color" value="#007bff">
            </div>
          </div>
          
          <!-- Embroidery Estimation -->
          <div class="tool-section">
            <h6><i class="fas fa-calculator me-2"></i>Stitch Estimation</h6>
            <div class="card">
              <div class="card-body p-3">
                <div class="d-flex justify-content-between">
                  <small>Estimated Stitches:</small>
                  <strong id="stitch-count">0</strong>
                </div>
                <div class="d-flex justify-content-between">
                  <small>Estimated Cost:</small>
                  <strong id="estimated-cost">$0.00</strong>
                </div>
                <button class="btn btn-sm btn-outline-primary w-100 mt-2" id="calculate-stitches">
                  <i class="fas fa-sync me-1"></i>Recalculate
                </button>
              </div>
            </div>
          </div>
          
          <!-- Actions -->
          <div class="tool-section">
            <h6><i class="fas fa-tools me-2"></i>Actions</h6>
            <button class="btn btn-outline-danger btn-tool" id="clear-design">
              <i class="fas fa-trash me-2"></i>Clear Design
            </button>
            <button class="btn btn-outline-secondary btn-tool" id="reset-view">
              <i class="fas fa-undo me-2"></i>Reset View
            </button>
          </div>
        </div>
      </div>
    </div>

    <%= yield %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- TUI Image Editor JS -->
    <script src="https://uicdn.toast.com/fabric/4.4.0/fabric.min.js"></script>
    <script src="https://uicdn.toast.com/tui-code-snippet/1.5.2/tui-code-snippet.min.js"></script>
    <script src="https://uicdn.toast.com/tui-color-picker/2.2.7/tui-color-picker.min.js"></script>
    <script src="https://uicdn.toast.com/tui-image-editor/3.15.2/tui-image-editor.min.js"></script>
    
    <%= yield :javascript %>
  </body>
</html>